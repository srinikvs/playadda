{
  "id": "A6",
  "layer": "unit",
  "title": "Version string present in package.json, VERSION, portal.js, and HTML stamp",
  "gate": "optional",
  "steps": [{ "op": "readVersionSources" }],
  "expect": [
    { "assert": "versionMatchesPackage" },
    { "assert": "htmlHasVersionTag", "file": "index.html" },
    { "assert": "uiUsesVersionLabel", "file": "js/portal.js" }
  ]
}
